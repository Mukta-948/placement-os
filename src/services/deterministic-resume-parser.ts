import type { ResumeEvidence, ResumeProfile } from "../types/resume.js"; import type { ResumeParser } from "./resume-parser.js";
export class DeterministicResumeParser implements ResumeParser { parse(userId:string,text:string,now:string):ResumeProfile { const lines=text.split(/\r?\n/); const bullets=lines.filter(x=>/^\s*[-•]/.test(x)); const evidence:ResumeEvidence={skills:[],bullets,quantifiedBullets:bullets.filter(x=>/\d|%/.test(x)).length,actionVerbBullets:bullets.filter(x=>/\b(built|led|developed|improved|designed)\b/i.test(x)).length}; const skills=(text.match(/\b(TypeScript|JavaScript|Python|Java|SQL|React|Node(?:\.js)?|AWS|Docker|Kubernetes)\b/gi)??[]).map(x=>x.toLowerCase()); return {schemaVersion:1,userId,lastUploadedAt:now,lastAnalyzedAt:now,skills:[...new Set(skills)],projects:/project/i.test(text)?[evidence]:[],experience:/experience|work/i.test(text)?[evidence]:[],sections:{skills:/skills/i.test(text),projects:/project/i.test(text),experience:/experience|work/i.test(text),education:/education/i.test(text)}}; } }

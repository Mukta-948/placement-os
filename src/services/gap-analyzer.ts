import type{ResumeProfile}from"../types/resume.js";import type{UserProfile}from"../types/profile.js";import type{LearningProgress}from"../types/learning-progress.js";export class GapAnalyzer{analyze(r:ResumeProfile,p:UserProfile,l:LearningProgress){return [...new Set([...p.skills,...Object.keys(l.topics)])].filter(x=>!r.skills.includes(x.toLowerCase())).map(x=>({area:x,severity:(l.topics[x]?.mastery??50)<40?"high":"medium",reason:"Capability is not evidenced in the resume"}))}}
